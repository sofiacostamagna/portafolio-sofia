/**
 * next.config.js
 * This file is used to configure the build and server behavior of Next.js.
 */

/**
 * @type {import('next').NextConfig}
 * The above line indicates that the file is of type NextConfig and uses TypeScript.
 * This provides autocompletion and type checking for Next.js configuration.
 * The `NextConfig` interface is defined by Next.js and describes available configuration options.
 */
const nextConfig = {};

// Export the Next.js configuration so it can be used by the Next build system.
module.exports = nextConfig;
